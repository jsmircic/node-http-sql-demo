{
  "name": "server-demo",
  "version": "1.0.0",
  "description": "just a demo",
  "main": "src/main.js",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node-dev --respawn  --require dotenv/config  ./src/main.ts dotenv_config_path=.env",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jurica Smircic",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.11.9",
    "prisma": "^4.5.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@prisma/client": "^4.5.0",
    "dotenv": "^16.0.3",
    "ts-node-dev": "^2.0.0"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  }
}
